name: cli
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.3.0
  logging: ^1.2.0
  command_runner:
    path: ../command_runner
  wikipedia:
    path: ../wikipedia